# Server Environment Variables

# Server Port
PORT=3000

# Node Environment (development or production)
NODE_ENV=development

# Database URL (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/database_name

# Better Auth Configuration
# This should be your deployed server URL (same as where the server is hosted)
BETTER_AUTH_URL=http://localhost:3000

# Generate a secure random string for production
# You can use: openssl rand -base64 32
BETTER_AUTH_SECRET=your_secret_key_here_min_32_chars

# Trusted Origins (comma-separated list of allowed client URLs)
# Development: http://localhost:5173
# Production: https://your-frontend-domain.com,https://www.your-frontend-domain.com
TRUSTED_ORIGINS=http://localhost:5173

# OpenAI API Key (if using AI features)
OPENAI_API_KEY=your_openai_api_key_here
